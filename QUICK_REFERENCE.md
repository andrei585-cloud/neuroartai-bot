# NeuroArtAI Bot - Быстрая справка

## 🚀 Запуск

```bash
python bot.py
```

## ✅ Проверка конфигурации

```bash
python test_env.py
```

Ожидаемый результат:
```
[OK] TELEGRAM_TOKEN
[OK] HF_API_TOKEN
[OK] python-telegram-bot
[OK] requests
[OK] Pillow
[OK] python-dotenv
[SUCCESS] SETUP COMPLETE!
```

## 📦 Установка зависимостей

```bash
pip install -r requirements.txt
```

## 🔍 Что должно быть в проекте

Обязательные файлы:
- ✓ `bot.py` - Основной файл
- ✓ `.env` - Токены (должен существовать!)
- ✓ `requirements.txt` - Зависимости
- ✓ `Procfile` - Для Heroku
- ✓ `.gitignore` - Исключает .env

## 🤖 Команды бота в Telegram

| Что отправить | Результат |
|---|---|
| `/start` | Приветствие и инструкции |
| Любой текст | Генерация изображения |

## 🎨 Примеры запросов

```
кошка
```

```
красивый закат над морем, реалистично
```

```
замок в облаках, фэнтези арт
```

```
кибер-панк город неоновые огни
```

## ⏱️ Ожидаемое время

- Первый запрос: 15-30 секунд
- Последующие: 10-20 секунд

## 🐛 Если что-то не работает

### Бот не запускается

```bash
# Проверить версию Python
python --version

# Переустановить зависимости
pip install -r requirements.txt --upgrade

# Проверить конфигурацию
python test_env.py
```

### Бот запущен, но не отвечает

- Убедитесь, что консоль показывает "Бот готов к работе!"
- Проверьте интернет соединение
- Отправьте `/start` боту

### Ошибка "неверный токен"

- Проверьте `.env` файл
- Убедитесь, что токен скопирован полностью
- Нет пробелов в начале/конце токена

### Ошибка API 503

Это нормально! Бесплатный сервер перегружен.
Попробуйте позже.

## 📝 Структура проекта

```
neuroartai-bot/
├── bot.py (запуск)
├── .env (токены)
├── requirements.txt (зависимости)
├── Procfile
├── .gitignore
└── README.md
```

## 🌐 Развёртывание на Heroku

```bash
heroku login
heroku create app-name
heroku config:set TELEGRAM_TOKEN=... HF_API_TOKEN=...
git add . && git commit -m "Deploy" && git push heroku main
heroku ps:scale worker=1
heroku logs --tail
```

## 🔐 Безопасность

✓ НИКОГДА не коммитьте `.env`  
✓ `.env` уже в `.gitignore`  
✓ Если токен утёк:
  1. Отозовите токен (BotFather / Hugging Face)
  2. Создайте новый
  3. Обновите `.env`
  4. Перезагрузите бота

## 📞 Документация

- `README.md` - Полное описание
- `TESTING.md` - Как тестировать
- `HEROKU_DEPLOY.md` - Развёртывание
- `PROJECT_SUMMARY.md` - Резюме проекта

## 💡 Полезные команды

```bash
# Проверить установленные пакеты
pip list

# Очистить кэш Python
rm -r __pycache__

# Посмотреть содержимое .env
type .env

# Создать папку images
mkdir images

# Удалить все .png файлы
del *.png
```

## 🎯 Первые шаги

1. Запустить: `python bot.py`
2. Открыть Telegram
3. Найти бота
4. Отправить `/start`
5. Отправить `красивый закат`
6. Получить изображение!

## ✨ Готово!

Ваш бот работает! Наслаждайтесь генерацией изображений! 🎨


