# Руководство по тестированию NeuroArtAI Bot

## Предварительная подготовка

1. **Убедитесь, что установлены зависимости:**
```bash
pip install -r requirements.txt
```

2. **Проверьте конфигурацию:**
```bash
python test_env.py
```

Должны увидеть:
- `[OK] TELEGRAM_TOKEN`
- `[OK] HF_API_TOKEN`
- Все зависимости отмечены как `[OK]`

## Запуск бота

### Вариант 1: Напрямую через Python
```bash
python bot.py
```

### Вариант 2: Через Windows batch (если есть .env)
```bash
run.bat
```

После запуска вы должны увидеть в консоли:
```
[INFO] Запускаю NeuroArtAI-бота...
[INFO] Обработчики установлены
[INFO] Бот готов к работе! Запускаю polling...
```

## Тестирование в Telegram

### Шаг 1: Найдите своего бота
- Откройте Telegram
- В поиске найдите вашего бота (имя, которое указали в BotFather)
- Или откройте чат по ссылке: `https://t.me/your_bot_username`

### Шаг 2: Протестируйте команду /start
**Отправьте:** `/start`

**Ожидаемый результат:**
- Бот ответит приветственным сообщением
- В консоли появится лог: `Пользователь [ID] запустил бота`

### Шаг 3: Протестируйте генерацию изображения

**Отправьте:** `красивый закат над морем, реалистично`

**Ожидаемые действия:**
1. Бот ответит: "⏳ Обрабатываю твой запрос..."
2. Через 10-30 секунд вышлет сгенерированное изображение
3. Под изображением будет подпись с вашим описанием

**В консоли вы увидите:**
```
[INFO] Пользователь [ID] запросил: красивый закат над морем, реалистично
[INFO] Отправляю запрос на API...
[INFO] Изображение успешно сгенерировано: images/generated_image_[ID]_[TIMESTAMP].png
```

## Примеры тестовых запросов

1. **Простое описание:**
   ```
   собака
   ```

2. **Описание с деталями:**
   ```
   кошка в шляпе, сидит на столе, реалистичная фотография
   ```

3. **Стилизованное описание:**
   ```
   замок в облаках, фэнтези арт, масляная живопись
   ```

4. **Абстрактное описание:**
   ```
   геометрические фигуры, неоновые цвета, цифровое искусство
   ```

## Возможные ошибки и решения

### Ошибка 1: Сообщение об ошибке "API перегружен" (503)
```
⏳ Сервис Hugging Face перегружен. Попробуй ещё раз через минуту.
```
**Решение:** Это нормально для бесплатного плана. Подождите и отправьте запрос ещё раз.

### Ошибка 2: Сообщение "Неверный токен" (401)
```
❌ Ошибка: Неверный токен API Hugging Face
```
**Решение:**
- Проверьте правильность `HF_API_TOKEN` в файле `.env`
- Скопируйте токен заново с сайта huggingface.co
- Перезагрузите бота

### Ошибка 3: Бот не отвечает на /start
**Решение:**
- Проверьте, работает ли скрипт (не было ошибок в консоли)
- Проверьте интернет соединение
- Проверьте правильность `TELEGRAM_TOKEN` в `.env`
- Перезагрузите бота: остановите (Ctrl+C) и запустите снова

### Ошибка 4: "Время ожидания истекло"
```
⏱️ Время ожидания истекло.
```
**Решение:**
- Попробуйте более короткое описание
- Попробуйте позже (сервер может быть перегружен)
- Увеличьте timeout в `bot.py` (строка `timeout=300`)

### Ошибка 5: Консоль показывает ошибки при запуске
**Решение:**
1. Убедитесь, что все пакеты установлены:
   ```bash
   pip install -r requirements.txt
   ```
2. Проверьте версию Python (нужна 3.8+):
   ```bash
   python --version
   ```

## Проверка логов

Когда бот запущен, обратите внимание на консоль. Там вы увидите:

- **Информационные сообщения** `[INFO]`:
  - Загрузка токенов
  - Установка обработчиков
  - Запуск polling
  - Входящие сообщения пользователей

- **Предупреждения** `[WARNING]`:
  - Ошибки API 503 (перегрузка сервера)

- **Ошибки** `[ERROR]`:
  - Отсутствие токенов
  - Неверный API токен
  - Другие критические ошибки

## Очистка сохранённых изображений

Сгенерированные изображения сохраняются в папке `images/`:

```bash
# Посмотреть список изображений
dir images

# Удалить все изображения
rmdir /s /q images
mkdir images
```

Или просто удалите нежелательные файлы вручную.

## Остановка бота

1. **В консоли:** Нажмите `Ctrl+C`
2. Бот корректно завершит работу

## Успешное тестирование

Если вы успешно:
- ✓ Запустили бота без ошибок
- ✓ Получили ответ на `/start`
- ✓ Сгенерировали изображение
- ✓ Видите логи в консоли

**Поздравляем! Ваш NeuroArtAI-бот работает корректно!** 🎉

---

**Следующие шаги:**
- Развертните на Heroku для облачного хостинга
- Добавьте дополнительные команды и функции
- Создайте вебинтерфейс для управления ботом

