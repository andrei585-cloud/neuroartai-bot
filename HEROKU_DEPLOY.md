# Развёртывание на Heroku

Руководство для развёртывания NeuroArtAI-бота на облачном сервисе Heroku для 24/7 работы.

## Подготовка

### 1. Создайте аккаунт на Heroku
- Перейдите на [heroku.com](https://heroku.com)
- Создайте новый аккаунт
- Подтвердите email

### 2. Установите Heroku CLI
**Windows:**
- Скачайте инсталлятор с [heroku.com/download](https://devcenter.heroku.com/articles/heroku-cli#install-the-heroku-cli)
- Запустите инсталлер
- Перезагрузитесь

**Проверка установки:**
```bash
heroku --version
```

### 3. Инициализируйте Git репозиторий
Если ещё не сделали:
```bash
cd D:\Projects\neuroartai-bot
git init
git config user.email "your-email@example.com"
git config user.name "Your Name"
git add .
git commit -m "Initial commit: NeuroArtAI Bot"
```

## Развёртывание

### Шаг 1: Вход в Heroku
```bash
heroku login
```

Браузер откроет окно для ввода логина.

### Шаг 2: Создайте приложение на Heroku
```bash
heroku create your-app-name
```

Замените `your-app-name` на уникальное имя для вашего приложения (например, `neuroartai-bot-123`).

**Результат:**
```
Creating app... done, https://neuroartai-bot-123.herokuapp.com
```

### Шаг 3: Установите переменные окружения
```bash
heroku config:set TELEGRAM_TOKEN=8400229648:AAGsp41ZXNEaVNzV2WP0N-W0IqJ2sXCyimg
heroku config:set HF_API_TOKEN=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
```

**Проверка:**
```bash
heroku config
```

### Шаг 4: Запустите worker dyno
По умолчанию Heroku запускает `web` процесс, но нам нужен `worker`:

```bash
heroku ps:scale worker=1
```

**Проверка процессов:**
```bash
heroku ps
```

Должно показать:
```
worker.1: up
```

### Шаг 5: Запушьте код
```bash
git push heroku main
```

Если ветка называется `master`:
```bash
git push heroku master
```

**Процесс:**
1. Heroku установит зависимости из `requirements.txt`
2. Запустит указанную в `Procfile` команду
3. Бот начнёт работать

## Мониторинг

### Просмотр логов
```bash
heroku logs --tail
```

Это покажет реальные логи работы бота. Нажмите `Ctrl+C` чтобы выйти.

### Просмотр истории логов
```bash
heroku logs --lines=50
```

### Просмотр одноразовых логов
```bash
heroku logs
```

## Управление ботом

### Остановка бота
```bash
heroku ps:scale worker=0
```

### Включение бота
```bash
heroku ps:scale worker=1
```

### Перезагрузка бота
```bash
heroku dyno:restart worker
```

### Просмотр статуса
```bash
heroku ps
```

## Обновление кода

1. **Внесите изменения локально**
```bash
# Отредактируйте файлы
```

2. **Коммитьте изменения**
```bash
git add .
git commit -m "Your commit message"
```

3. **Запушьте на Heroku**
```bash
git push heroku main
```

Бот автоматически перезагрузится с новым кодом.

## Проблемы и решения

### Проблема: "No such file or directory: 'bot.py'"
**Решение:**
- Убедитесь, что `bot.py` находится в корневой папке
- Проверьте написание в `Procfile`

### Проблема: "ModuleNotFoundError"
**Решение:**
- Убедитесь, что все зависимости в `requirements.txt`
- Обновите: `git add requirements.txt && git commit -m "Update requirements" && git push heroku main`

### Проблема: "Your app crashed"
**Решение:**
- Посмотрите логи: `heroku logs --tail`
- Проверьте токены: `heroku config`
- Перезагрузитесь: `heroku dyno:restart worker`

### Проблема: "Неверный токен"
**Решение:**
```bash
heroku config:set TELEGRAM_TOKEN=your_new_token
heroku config:set HF_API_TOKEN=your_new_token
heroku dyno:restart worker
```

### Проблема: "Bad Gateway" ошибки
**Решение:**
- Обычно это временные сбои Heroku
- Дождитесь несколько минут
- Если проблема сохраняется, перезагрузите: `heroku dyno:restart worker`

## Удаление приложения

```bash
heroku apps:destroy --app your-app-name
```

Это удалит все данные приложения.

## Дополнительные настройки

### Масштабирование
Heroku предоставляет бесплатный уровень с ограничениями. Для лучшей производительности:

```bash
# Перейти на платный план
heroku dyno:type ecodynamo --app your-app-name
```

### Логирование с Papertrail
Heroku интегрируется с Papertrail для расширенного логирования:

```bash
heroku addons:create papertrail
heroku addons:open papertrail
```

### Резервные копии
Если вы используете базу данных:

```bash
heroku pg:backups create
heroku pg:backups download b001
```

## Контроль затрат

Heroku предоставляет:
- ✓ 5 бесплатных `dyno` часов в месяц
- ✓ Бесплатные `add-ons` (до определённых лимитов)
- ⚠️ После истечения бесплатных часов требуется платная карта

**Совет:** Используйте `heroku ps` для просмотра использованных часов.

## Команды для быстрого запуска

```bash
# Инициализация (первый раз)
heroku login
heroku create your-app-name
heroku config:set TELEGRAM_TOKEN=your_token HF_API_TOKEN=your_token
git add . && git commit -m "Initial commit" && git push heroku main
heroku ps:scale worker=1

# Просмотр логов
heroku logs --tail

# Обновление кода
git add . && git commit -m "Update" && git push heroku main

# Перезагрузка
heroku dyno:restart worker

# Остановка
heroku ps:scale worker=0
```

## Успешное развёртывание

Если вы видите в логах:
```
Запускаю NeuroArtAI-бота...
Обработчики установлены
Бот готов к работе! Запускаю polling...
```

**Поздравляем! Ваш бот работает на Heroku 24/7!** 🚀

---

## Дополнительные ресурсы

- [Документация Heroku](https://devcenter.heroku.com/)
- [Heroku Python Guide](https://devcenter.heroku.com/articles/getting-started-with-python)
- [Procfile Documentation](https://devcenter.heroku.com/articles/procfile)

